import 'package:flutter/material.dart';

class AppConstants {
  // Primary and Secondary Colors
  static const Color primaryColor = Color.fromARGB(255, 27, 110, 177); // Blue color
  static const Color secondaryColor = Color(0xFFffffff  ); // Light grey
  
  // Scaffold Color
  static const Color scaffoldColor = Color(0xFFf1f1f1); // White color

  // Font Sizes
  static const double headingFontSize = 24.0;
  static const double subHeadingFontSize = 20.0;
  static const double bodyFontSize = 16.0;
  static const double smallTextFontSize = 12.0;
  
  // Add more constants as needed
}
